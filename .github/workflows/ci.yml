name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout código
      uses: actions/checkout@v4

    - name: Instalar Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'

    - name: Instalar dependencias
      run: |
        npm install
        echo "No hay dependencias por ahora"

    - name: Ejecutar pruebas (simuladas)
      run: |
        echo "Ejecutando pruebas ficticias"
        echo "OK - Todas las pruebas pasaron"
